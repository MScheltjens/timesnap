'use client';

import { TDBImg } from '@/types/types';

type Props = {
    imgData: TDBImg[];
};

export const ImageSlider = ({ imgData }: Props) => {
    console.log(imgData);
    return <section></section>;
};
